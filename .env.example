# PostgreSQL Database Configuration
DB_NAME=cryptoAI
DB_USER=postgres
DB_PASSWORD=your_secure_password_here

# Copy this file to .env and update the values for your environment
# .env should be in .gitignore (never commit real passwords)
